import pygame

class TimeManager:
        def __init__(self, clock: pygame.time.Clock = None):
                if clock is None:
                        clock = pygame.time.Clock()
                self.clock = clock